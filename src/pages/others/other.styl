.others-page {
  position: relative;
}
.title {
  position: absolute;
  top: 0;
  left: 10px;
  z-index: 99;
  >img {
    width 80%
    display: block;
  }
}

.other-title-bg {
  background-repeat: no-repeat;
  background-position: center top;
  height: 401px;
  width: 100%;
}